export enum statusProduct {
  ACTIVE = "ACTIVE",
  INACTIVE = "INACTIVE",
  OUTOFSTOCK = "OUTOFSTOCK",
}

export enum typeNavMenuItem {
  DYNAMIC = "dynamic",
  STATIC = "static",
}
export enum linkType {
  PRODUCTS = "products",
  STORES = "stores",
}
